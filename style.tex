%% from scribble/lncs
\renewcommand{\titleAndVersionAndAuthors}[3]{\title{#1}#3\maketitle}
\renewcommand{\titleAndEmptyVersionAndAuthors}[3]{\titleAndVersionAndAuthors{#1}{#1}{#3}}

\newcommand{\lipicsauthor}[2]{\author[#1]{#2}}
\newcommand{\lipicsaffil}[2]{\affil[#1]{#2}}

\renewcommand{\SAuthorSep}{}

\newcommand{\lipicsabstract}[1]{
  \begin{abstract}
    #1
  \end{abstract}
}

%% For bibliography

%% hyper-refs for bibliography
\newcommand{\Thyperref}[2]{\hyperref[#2]{#1}}

%% Use the lipics bibliography macro
\renewenvironment{AutoBibliography}
{\begin{thebibliography}{50}} % FIXME: 50?
{\end{thebibliography}}

%% make this a no-op to make the bibliography show correctly
%% (for single-column table output)
\renewenvironment{SingleColumn}{}{}

%% to get rid of the redundant "Bibliography" section header
\renewcommand{\Ssectionstarx}[2]{}

%% For figures

\DeclareCaptionFormat{myformat}{#3}
\captionsetup{format=myformat}

%% replace the colon that Scribble puts in that we don't want
\def\decolonize#1:{#1}

\renewcommand{\FigureTarget}[2]{%
\kern0.05em{\color[rgb]{0.99,0.78,0.07}\rule{0.73em}{0.73em}}%
\hspace*{0.67em}\textsf{\textbf{\decolonize#1\relax} % match font styling
% to mimic the spacing in LIPIcs figure captions
\hspace{-0.5em}}
}

\renewcommand{\Legend}[1]{~

                          \hrule width \hsize height .33pt
                          \vspace{4pt}
                          \caption{#1}}

%% Acknowledgments macro
\newcommand{\acknowledgments}[1]{\subparagraph*{Acknowledgments} #1}

%% Restore the saved definitions of the array package. We
%% have to do this since stabular redefines them
\makeatletter
  \let\@mkpream\@tmpmkpream
  \let\@addamp\@tmpaddamp
\makeatother

%% Override bigtabular to use tabular (doesn't conflict with array)
\renewenvironment{bigtabular}{\begin{tabular}}{\end{tabular}}
